import CSS from "csstype";

const NavBar: React.FC = () => {

    const screenStyle: CSS.Properties = {
        backgroundColor: "#FFF",
        
    }
    
    return (
        <div>

        </div>
    );
}
export default NavBar;
import CSS from "csstype";
import styles from "./barButton.module.css";

type MyMethodType = (arg: string) => void;

interface navBarProps {
}
const NavBar: React.FC = () => {
    
    return (
        <div>
            <button></button>
        </div>
    );
}
export default NavBar;